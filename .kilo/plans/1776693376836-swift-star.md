# Plan de Implementación - Módulo Admin REST

---

## DASHBOARD IMPLEMENTATION - Priority 1

### Estado Actual

El endpoint GET /dashboard YA ESTÁ IMPLEMENTADO en:

- `src/dashboard/dashboard.controller.ts`
- `src/dashboard/dashboard.service.ts`
- `src/dashboard/dashboard.module.ts`

### Datos que devuelve actualmente:

```json
{
  "dailySales": {
    "date": "2026-04-20",
    "todayTotalSales": 0,
    "todayOrdersCount": 0,
    "currency": "COP"
  },
  "accountClosings": [
    { "date": "...", "totalIncome": 0, "totalExpenses": 0, "balance": 0 }
  ],
  "menu": { "categoriesCount": 0, "productsCount": 0 },
  "tables": { "spaces": [], "totalTables": 0 },
  "users": { "recentUsers": [], "totalUsers": 0 }
}
```

### Posibles Issues a verificar:

1. El campo `name` vs `firstName`/`lastName` en User - el dashboard busca `firstName`, `lastName` pero el modelo tiene ambos campos
2. timezone - usa GMT-5 (Colombia)

### Acción requerida:

- [ ] Probar endpoint con curl y verificar respuesta
- [ ] Si hay errores, corregir el servicio

---

## Estado Actual (YA IMPLEMENTADO)

### Auth ✅

- `POST /auth/login`
- `POST /auth/create-user`
- `POST /auth/recover-password`
- `POST /auth/reset-password`
- `GET /auth/profile`

### Zones ✅

- `GET /zones`
- `GET /zones/:id`
- `POST /zones`
- `PUT /zones/:id`
- `DELETE /zones/:id`

### Tables ✅

- `GET /tables`
- `GET /zones/:zoneId/tables`
- `POST /tables`
- `DELETE /tables/:id`

### Categories ✅

- `GET /categories`
- `GET /categories/:id`
- `POST /categories`
- `PUT /categories/:id`
- `DELETE /categories/:id`

### Products ✅

- `GET /categories/:categoryId/products`
- `GET /products/:id`
- `POST /products`
- `PUT /products/:id`
- `DELETE /products/:id`

### Users ✅

- `GET /users`
- `GET /users/:id`

### Dashboard ✅ (SIN RESTRICCIÓN)

- `GET /dashboard` (requiere JWT, sin restricción de rol)

---

## Nueva Tarea 1: Dashboard + Dev Login

### 1.1 Agregar restricción rol ADMIN al dashboard

**Archivo:** `src/dashboard/dashboard.controller.ts`

**Cambios:**

1. Importar `Roles` decorator y `RolesGuard`
2. Agregar `@Roles('ADMIN')` al método getDashboard

### 1.2 Crear endpoint POST /auth/dev-login

**Propósito:** JWT token para desarrollo sin contraseña

**Restricciones:**

- Solo funciona desde localhost (127.0.0.1, ::1)
- No accesible desde producción

**Archivos:**

- `src/auth/auth.controller.ts` - agregar endpoint
- `src/auth/auth.service.ts` - agregar método devLogin()

**Request:**

```json
{ "email": "admin@lalora.com" }
```

**Response:**

```json
{
  "access_token": "eyJ...",
  "role": "ADMIN"
}
```

---

## Tareas Pendientes

### 1. Expenses Module (CRUD)

**Archivos a crear:**

- `src/expenses/expenses.module.ts`
- `src/expenses/expenses.service.ts`
- `src/expenses/expenses.controller.ts`
- `src/expenses/dto/create-expense.dto.ts`

**Endpoints:**

- `GET /expenses` - Listar gastos (con filtros por fecha)
- `POST /expenses` - Crear gasto
- `DELETE /expenses/:id` - Eliminar gasto

**Implementación:**

1. Crear module con PrismaService inyectado
2. Crear service con métodos: findAll(), create(), delete()
3. Crear controller con rutas CRUD
4. Registrar en AppModule
5. Actualizar Swagger

### 2. Users - Completar CRUD

**Archivos a modificar:**

- `src/users/users.controller.ts`
- `src/users/users.service.ts`

**Endpoints a agregar:**

- `PUT /users/:id` - Actualizar usuario
- `DELETE /users/:id` - Soft delete usuario

### 3. Auth - Change Password

**Archivos a modificar:**

- `src/auth/auth.controller.ts`
- `src/auth/auth.service.ts`
- `src/auth/dto/change-password.dto.ts` (nuevo)

**Endpoints:**

- `POST /auth/change-password` - Cambiar contraseña (requiere JWT)

### 4. Reports Module

**Archivos a crear:**

- `src/reports/reports.module.ts`
- `src/reports/reports.service.ts`
- `src/reports/reports.controller.ts`

**Endpoints:**

- `GET /reports/sales` - Reporte de ventas
- `GET /reports/expenses` - Reporte de gastos
- `GET /reports/products` - Productos más vendidos
- `GET /reports/orders-by-hour` - Pedidos por hora
- `GET /reports/tables` - Occupancy de mesas

---

## Orden de Implementación

1. **Expenses Module** - Más simple, modelo ya existe
2. **Users update/delete** - Extensión de módulo existente
3. **Auth change-password** - Extensión de auth
4. **Reports Module** - Más complejo, depende de datos existentes

---

## Após Implementación

1. Build: `npm run build`
2. Probar cada endpoint
3. Verificar Swagger: `http://localhost:3000/api`
